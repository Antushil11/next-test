import Title from '@/components/Title';
import React from 'react';

const page = () => {
    return (
        <div>
            <Title>This is Dashboard</Title>
        </div>
    );
};

export default page;